version: "3.5"

services:
  mongo:
    container_name: mongo
    image: mongo:latest
    networks:
      - prod

  app:
    container_name: app
    environment:
      - DB_HOST=mongo
      - PORT=3000
    restart: always
    build: .
    ports:
      - 3000:3000
    depends_on:
      - mongo
    networks:
      - prod

networks:
  prod:
    name: prod
    driver: bridge